---
- name: Configure elastic repository
  ansible.builtin.include_role:
    name: nephelaiio.elastic_repo
  when: metricbeat_repo_manage

- name: Collect package facts
  ansible.builtin.package_facts:
    manager: auto

- name: Disable package locks
  when: metricbeat_package_hold
  block:
    - name: Disable yum package lock
      when: ansible_os_family == 'RedHat'
      block:
        - name: Install yum version lock support
          ansible.builtin.package:
            name: yum-plugin-versionlock

        - name: Disable yum package lock
          community.general.yum_versionlock:
            name: "{{ item }}"
            state: absent
          loop: "{{ [metricbeat_package_names] | flatten }}"
          changed_when: false
          when: item in packages

    - name: Disable apt package lock
      ansible.builtin.dpkg_selections:
        name: "{{ item }}"
        selection: install
      loop: "{{ [metricbeat_package_names] | flatten }}"
      changed_when: false
      when:
        - item in packages
        - ansible_os_family == 'Debian'

- name: Install metricbeat packages
  ansible.builtin.package:
    name: "{{ metricbeat_package_names }}"
    state: "{{ metricbeat_package_state }}"

- name: Configure metricbeat
  when: metricbeat_package_state in ['present', 'latest']
  block:
    - name: Enable package locks
      when: metricbeat_package_hold
      block:
        - name: Enable apt package hold
          ansible.builtin.dpkg_selections:
            name: "{{ item }}"
            selection: hold
          loop: "{{ [metricbeat_package_names] | flatten }}"
          changed_when: false
          when: ansible_os_family == 'Debian'

        - name: Enable yum package lock
          community.general.yum_versionlock:
            name: "{{ item }}"
            state: present
          loop: "{{ [metricbeat_package_names] | flatten }}"
          changed_when: false
          when: ansible_os_family == 'RedHat'

    - name: Manage metricbeat main configuration file
      ansible.builtin.copy:
        content: |
          # Ansible managed

          {{ metricbeat_conf | to_nice_yaml(indent=2) }}
        dest: "{{ metricbeat_conf_path }}"
        mode: "u=rw,g=r,o="
      notify: metricbeat_restart

    - name: Manage metricbeat module configuration list
      ansible.builtin.copy:
        dest: "{{ metricbeat_conf_modules_path }}/{{ item.key }}.yml"
        content: |
          ---
          {{ item.value | to_nice_yaml(indent=2) }}
        mode: "u=rw,g=r,o="
      loop_control:
        label: "{{ item.key }}"
      loop: "{{ metricbeat_conf_modules | dict2items }}"
      when: metricbeat_conf_manage
      notify: metricbeat_restart

    - name: Manage metricbeat services
      ansible.builtin.service:
        name: "{{ metricbeat_service_name }}"
        state: "{{ metricbeat_service_status }}"
        enabled: "{{ metricbeat_service_enabled }}"

    - name: List disabled modules
      ansible.builtin.find:
        paths: "{{ metricbeat_conf_modules_path }}"
        patterns: "*.disabled"
      register: metricbeat_disabled_modules

    - name: Remove disabled modules
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop: "{{ metricbeat_disabled_modules.files | map(attribute='path') }}"

    - name: Remove installation artifacts
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - "{{ metricbeat_conf_path | dirname }}/metricbeat.reference.yml"
        - "{{ metricbeat_conf_path }}.dpkg-dist"
